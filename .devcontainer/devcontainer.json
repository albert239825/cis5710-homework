{
  "image": "cis5710/hw-base-gradescope:v2",
  "customizations": {
    "vscode": {
      "extensions": [
        "mshr-h.veriloghdl",
        "lramseyer.vaporview",
        "ms-python.python"
      ]
    }
  },

  // allow ssh agent for using host machine ssh keys
  "mounts": [
    // Mount keys (read-only)
    "type=bind,source=${localEnv:HOME}/.ssh,target=/home/penn/.ssh,readonly"
  ]
}