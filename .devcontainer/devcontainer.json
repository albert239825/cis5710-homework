{
  "image": "cis5710/hw-base-gradescope:v2",
  "customizations": {
    "vscode": {
      "extensions": [
        "mshr-h.veriloghdl",
        "lramseyer.vaporview",
        "ms-python.python"
      ]
    }
  },
  // allow ssh agent for using host machine ssh keys
  "mounts": [
    // Mount keys (read-only) - target must match the user running in the container (root)
    "type=bind,source=${localEnv:HOME}/.ssh,target=/root/.ssh,readonly"
  ]
}